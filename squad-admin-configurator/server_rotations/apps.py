from django.apps import AppConfig


class ServerRotationsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "server_rotations"
    verbose_name = "3. Серверные ротации"
